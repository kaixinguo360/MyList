<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.list.system.mapper.GroupMapper">
  <resultMap id="BaseResultMap" type="com.my.list.system.mapper.Group">
    <id column="id" jdbcType="BIGINT" property="id"/>
    <result column="user" jdbcType="BIGINT" property="user"/>
    <result column="ctime" jdbcType="TIMESTAMP" property="ctime"/>
    <result column="mtime" jdbcType="TIMESTAMP" property="mtime"/>
    <result column="name" jdbcType="VARCHAR" property="name"/>
    <result column="description" jdbcType="VARCHAR" property="description"/>
    <result column="name" jdbcType="VARCHAR" property="type"/>
    <result column="description" jdbcType="VARCHAR" property="value"/>
  </resultMap>

  <!-- Single Crud -->
  <insert id="insert" keyColumn="id" keyProperty="group.id" useGeneratedKeys="true">
    insert into `groups` (
        id,
        user,
        ctime,
        mtime,
        name,
        description,
        type,
        value
    )
    values (
               #{group.id,jdbcType=BIGINT},
               #{user.id,jdbcType=BIGINT},
               #{group.ctime,jdbcType=TIMESTAMP},
               #{group.mtime,jdbcType=TIMESTAMP},
               #{group.name,jdbcType=VARCHAR},
               #{group.description,jdbcType=VARCHAR},
               #{type,jdbcType=VARCHAR},
               #{group.value,jdbcType=VARCHAR}
    )
  </insert>
  <select id="select" resultMap="BaseResultMap">
    select *
    from `groups`
    where name = #{name,jdbcType=VARCHAR} and type = #{type,jdbcType=VARCHAR} and user = #{user.id,jdbcType=BIGINT}
  </select>
  <update id="update">
    update `groups`
    set id              = #{group.id,jdbcType=BIGINT},
        user            = #{group.user,jdbcType=BIGINT},
        mtime           = #{group.mtime,jdbcType=TIMESTAMP},
        name            = #{group.name,jdbcType=VARCHAR},
        description     = #{group.description,jdbcType=VARCHAR},
        value           = #{group.value,jdbcType=VARCHAR}
    where id = #{group.id,jdbcType=BIGINT} and type = #{type,jdbcType=VARCHAR} and user = #{user.id,jdbcType=BIGINT}
  </update>
  <delete id="delete">
    delete
    from `groups`
    where name = #{name,jdbcType=VARCHAR} and type = #{type,jdbcType=VARCHAR} and user = #{user.id,jdbcType=BIGINT}
  </delete>

  <!-- selectAll -->
  <select id="selectAll" resultMap="BaseResultMap">
    select * from `groups`
    where type = #{type,jdbcType=VARCHAR} and user = #{user.id,jdbcType=BIGINT}
    limit #{limit,jdbcType=INTEGER} offset #{offset,jdbcType=INTEGER}
  </select>

</mapper>
